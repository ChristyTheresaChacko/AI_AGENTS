# Example .env file
# Set MAIL_BACKEND to 'smtp' to force SMTP even if some vars look missing
MAIL_BACKEND=auto
SMTP_SERVER=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your_username
SMTP_PASSWORD=your_password
SMTP_FROM_EMAIL=no-reply@example.com
